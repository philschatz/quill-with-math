{
  "name": "quill-with-math",
  "version": "0.0.1",
  "description": "QuillJS with support for editing Math using KaTeX",
  "main": "index.js",
  "scripts": {
    "prepublish": "coffee -o ./build/ -c ./src/*.coffee",
    "install": "coffee -o ./build/ -c ./src/*.coffee",
    "test": "coffee -o ./build/ -c ./src/*.coffee && browserify ./build/index.js -o ./test-build.js"
  },
  "author": "",
  "dependencies": {
    "quill": "philschatz/quill#normalizer"
  },
  "devDependencies": {
    "coffeeify": "^1.0.0",
    "dom": "0.0.3",
    "katex": "^0.1.1",
    "lodash": "^2.4.1"
  }
}
